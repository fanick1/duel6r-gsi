{
  "type": "module",
  "comment:": [
    "Run npm start or npm run server to start server",
    "Run npm run server to start http server (localhost:3000)",
    "server accepts and stores data via POST to /api/duel6r-gsi",
    "server can provide the data via GET request to /api/duel6r-gsi"
  ],
  "name": "duel6r-gsi",
  "description": "duel6r game state integration backend",
  "author": "Frantisek Veverka",
  "main": "server.js",
  "scripts": {
    "es": "eslint --init",
    "tsc": "tsc",
    "start": "tsc && node dist/server.js"
  },
  "devDependencies": {
    "@types/express": "^4.17.1",
    "@types/express-ws": "^3.0.1",
    "@types/node": "18.11.10",
    "@typescript-eslint/eslint-plugin": "^5.45.0",
    "@typescript-eslint/parser": "^5.45.0",
    "eslint": "^8.29.0",
    "typescript": "^4.9.3"
  },
  "dependencies": {
    "body-parser": "^1.19.0",
    "express": "^4.17.1",
    "express-ws": "^5.0.2",
    "serve-static": "^1.14.1"
  },
  "private": true
}
